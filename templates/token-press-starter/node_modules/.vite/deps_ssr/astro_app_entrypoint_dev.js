import {
  BaseApp,
  createConsoleLogger,
  createModuleScriptElement,
  createStylesheetElementSet,
  ensure404Route,
  matchAllRoutes,
  routeComparator
} from "./chunk-LSP5GO3Z.js";
import "./chunk-PV5IKP6K.js";
import "./chunk-3ZV67HPR.js";
import "./chunk-FMWHJTKU.js";
import {
  Pipeline,
  findRouteToRewrite,
  getProps
} from "./chunk-XJPI77TF.js";
import {
  newNodePool
} from "./chunk-USHKIIF6.js";
import {
  HTMLStringCache
} from "./chunk-UA7MN74Z.js";
import {
  getCustom404Route,
  getCustom500Route,
  routeIsRedirect
} from "./chunk-HNDYL2KF.js";
import "./chunk-EGRHWZRV.js";
import "./chunk-TRT6TUAE.js";
import "./chunk-3PTCBMEQ.js";
import "./chunk-LFQIKYFL.js";
import "./chunk-PCPCYFEE.js";
import "./chunk-VZVNX2VY.js";
import "./chunk-2SC3GTRV.js";
import "./chunk-YWD6PRRP.js";
import "./chunk-F5OXOEIF.js";
import "./chunk-YFFDBQ5F.js";
import "./chunk-XMFONZN2.js";
import "./chunk-IT5ZF5A3.js";
import "./chunk-MMSX4JQT.js";
import "./chunk-ZOSZ4JYZ.js";
import "./chunk-2X4SSDLG.js";
import "./chunk-BWAA7FDP.js";
import "./chunk-SIU35MPB.js";
import "./chunk-LPY2RFCK.js";
import "./chunk-Q5EGYFIG.js";
import "./chunk-SXHTO4MG.js";
import "./chunk-TMMYQQ2M.js";
import "./chunk-AX4PI47U.js";
import "./chunk-GZSB5Q3H.js";
import "./chunk-MMB37DL7.js";
import "./chunk-6CI22L65.js";
import "./chunk-PNXQGC6T.js";
import {
  s
} from "./chunk-7JBXEF45.js";
import "./chunk-4QMAEQNT.js";
import "./chunk-FFCYXTXG.js";
import "./chunk-DLNGAAZA.js";
import "./chunk-ZJU56T3D.js";
import "./chunk-DOENTQOG.js";
import "./chunk-66G7VFBL.js";
import "./chunk-VOH3BLB4.js";
import {
  queueRenderingEnabled
} from "./chunk-6FGOOCQO.js";
import "./chunk-AJNM6R2I.js";
import "./chunk-R5GNQ4NZ.js";
import {
  ASTRO_VERSION
} from "./chunk-OZM3PXOQ.js";
import "./chunk-NLXTZJFN.js";
import "./chunk-3OWWBTIR.js";
import "./chunk-EGRHWZRV.js";
import {
  MiddlewareNoDataOrNextCalled,
  MiddlewareNotAResponse,
  NoMatchingStaticPathFound,
  isAstroError
} from "./chunk-F7CPYL62.js";
import "./chunk-Y2BVNUCK.js";
import "./chunk-PR4QN5HX.js";

// ../../node_modules/.pnpm/astro@6.0.1_@types+node@24.10.13_jiti@2.6.1_lightningcss@1.31.1_rollup@4.55.2_tsx@4.21._dedb2502a3da963a245a4af4094aacf2/node_modules/astro/dist/core/app/entrypoints/virtual/dev.js
import { manifest } from "virtual:astro:manifest";

// ../../node_modules/.pnpm/astro@6.0.1_@types+node@24.10.13_jiti@2.6.1_lightningcss@1.31.1_rollup@4.55.2_tsx@4.21._dedb2502a3da963a245a4af4094aacf2/node_modules/astro/dist/core/app/dev/pipeline.js
var NonRunnablePipeline = class _NonRunnablePipeline extends Pipeline {
  getName() {
    return "NonRunnablePipeline";
  }
  static create({ logger, manifest: manifest2, streaming }) {
    async function resolve(specifier) {
      if (specifier.startsWith("/")) {
        return specifier;
      } else {
        return "/@id/" + specifier;
      }
    }
    const pipeline = new _NonRunnablePipeline(
      logger,
      manifest2,
      "development",
      manifest2.renderers,
      resolve,
      streaming,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0,
      void 0
    );
    if (queueRenderingEnabled(manifest2.experimentalQueuedRendering)) {
      pipeline.nodePool = newNodePool(manifest2.experimentalQueuedRendering);
      if (manifest2.experimentalQueuedRendering.contentCache) {
        pipeline.htmlStringCache = new HTMLStringCache(1e3);
      }
    }
    return pipeline;
  }
  async headElements(routeData) {
    const { assetsPrefix, base } = this.manifest;
    const routeInfo = this.manifest.routes.find((route) => route.routeData === routeData);
    const links = /* @__PURE__ */ new Set();
    const scripts = /* @__PURE__ */ new Set();
    const styles = createStylesheetElementSet(routeInfo?.styles ?? [], base, assetsPrefix);
    for (const script of routeInfo?.scripts ?? []) {
      if ("stage" in script) {
        if (script.stage === "head-inline") {
          scripts.add({
            props: {},
            children: script.children
          });
        }
      } else {
        scripts.add(createModuleScriptElement(script));
      }
    }
    scripts.add({
      props: { type: "module", src: "/@vite/client" },
      children: ""
    });
    if (this.manifest.devToolbar.enabled) {
      scripts.add({
        props: {
          type: "module",
          src: "/@id/astro/runtime/client/dev-toolbar/entrypoint.js"
        },
        children: ""
      });
      const additionalMetadata = {
        root: this.manifest.rootDir.toString(),
        version: ASTRO_VERSION,
        latestAstroVersion: this.manifest.devToolbar.latestAstroVersion,
        debugInfo: this.manifest.devToolbar.debugInfoOutput ?? "",
        placement: this.manifest.devToolbar.placement
      };
      const children = `window.__astro_dev_toolbar__ = ${JSON.stringify(additionalMetadata)}`;
      scripts.add({ props: {}, children });
    }
    const { devCSSMap } = await import("virtual:astro:dev-css-all");
    const importer = devCSSMap.get(routeData.component);
    let css = /* @__PURE__ */ new Set();
    if (importer) {
      const cssModule = await importer();
      css = cssModule.css;
    } else {
      this.logger.warn(
        "assets",
        `Unable to find CSS for ${routeData.component}. This is likely a bug in Astro.`
      );
    }
    for (const { id, url: src, content } of css) {
      scripts.add({ props: { type: "module", src }, children: "" });
      styles.add({ props: { "data-vite-dev-id": id }, children: content });
    }
    return { scripts, styles, links };
  }
  componentMetadata() {
  }
  async getComponentByRoute(routeData) {
    try {
      const module2 = await this.getModuleForRoute(routeData);
      return module2.page();
    } catch {
    }
    const url = new URL(routeData.component, this.manifest.rootDir);
    const module = await import(
      /* @vite-ignore */
      url.toString()
    );
    return module;
  }
  async tryRewrite(payload, request) {
    const { newUrl, pathname, routeData } = findRouteToRewrite({
      payload,
      request,
      routes: this.manifest?.routes.map((r) => r.routeData),
      trailingSlash: this.manifest.trailingSlash,
      buildFormat: this.manifest.buildFormat,
      base: this.manifest.base,
      outDir: this.manifest?.serverLike ? this.manifest.buildClientDir : this.manifest.outDir
    });
    const componentInstance = await this.getComponentByRoute(routeData);
    return { newUrl, pathname, componentInstance, routeData };
  }
};

// ../../node_modules/.pnpm/astro@6.0.1_@types+node@24.10.13_jiti@2.6.1_lightningcss@1.31.1_rollup@4.55.2_tsx@4.21._dedb2502a3da963a245a4af4094aacf2/node_modules/astro/dist/prerender/routing.js
function getSortedPreloadedMatches({
  pipeline,
  matches,
  manifest: manifest2
}) {
  return preloadAndSetPrerenderStatus({
    pipeline,
    matches,
    manifest: manifest2
  }).sort((a, b) => routeComparator(a.route, b.route)).sort((a, b) => prioritizePrerenderedMatchesComparator(a.route, b.route));
}
function preloadAndSetPrerenderStatus({
  matches,
  manifest: manifest2
}) {
  const preloaded = new Array();
  for (const route of matches) {
    const filePath = new URL(`./${route.component}`, manifest2.rootDir);
    if (routeIsRedirect(route)) {
      preloaded.push({
        route,
        filePath
      });
      continue;
    }
    preloaded.push({ route, filePath });
  }
  return preloaded;
}
function prioritizePrerenderedMatchesComparator(a, b) {
  if (areRegexesEqual(a.pattern, b.pattern)) {
    if (a.prerender !== b.prerender) {
      return a.prerender ? -1 : 1;
    }
    return a.component < b.component ? -1 : 1;
  }
  return 0;
}
function areRegexesEqual(regexp1, regexp2) {
  return regexp1.source === regexp2.source && regexp1.global === regexp2.global;
}

// ../../node_modules/.pnpm/astro@6.0.1_@types+node@24.10.13_jiti@2.6.1_lightningcss@1.31.1_rollup@4.55.2_tsx@4.21._dedb2502a3da963a245a4af4094aacf2/node_modules/astro/dist/core/routing/dev.js
async function matchRoute(pathname, routesList, pipeline, manifest2) {
  const { logger, routeCache } = pipeline;
  const matches = matchAllRoutes(pathname, routesList);
  const preloadedMatches = getSortedPreloadedMatches({
    pipeline,
    matches,
    manifest: manifest2
  });
  for await (const { route: maybeRoute, filePath } of preloadedMatches) {
    try {
      await getProps({
        mod: await pipeline.getComponentByRoute(maybeRoute),
        routeData: maybeRoute,
        routeCache,
        pathname,
        logger,
        serverLike: pipeline.manifest.serverLike,
        base: manifest2.base,
        trailingSlash: manifest2.trailingSlash
      });
      return {
        route: maybeRoute,
        filePath,
        resolvedPathname: pathname
      };
    } catch (e) {
      if (isAstroError(e) && e.title === NoMatchingStaticPathFound.title) {
        continue;
      }
      throw e;
    }
  }
  const altPathname = pathname.replace(/\/index\.html$/, "/").replace(/\.html$/, "");
  if (altPathname !== pathname) {
    return await matchRoute(altPathname, routesList, pipeline, manifest2);
  }
  if (matches.length) {
    const possibleRoutes = matches.flatMap((route) => route.component);
    logger.warn(
      "router",
      `${NoMatchingStaticPathFound.message(
        pathname
      )}

${NoMatchingStaticPathFound.hint(possibleRoutes)}`
    );
  }
  const custom404 = getCustom404Route(routesList);
  if (custom404) {
    const filePath = new URL(`./${custom404.component}`, manifest2.rootDir);
    return {
      route: custom404,
      filePath,
      resolvedPathname: pathname
    };
  }
  return void 0;
}

// ../../node_modules/.pnpm/astro@6.0.1_@types+node@24.10.13_jiti@2.6.1_lightningcss@1.31.1_rollup@4.55.2_tsx@4.21._dedb2502a3da963a245a4af4094aacf2/node_modules/astro/dist/core/messages/runtime.js
var {
  bgGreen,
  bgYellow,
  bgCyan,
  bgWhite,
  black,
  blue,
  bold,
  cyan,
  dim,
  green,
  red,
  underline,
  yellow
} = s;
function req({
  url,
  method,
  statusCode,
  reqTime,
  isRewrite
}) {
  const color = statusCode >= 500 ? red : statusCode >= 300 ? yellow : blue;
  return color(`[${statusCode}]`) + ` ${isRewrite ? color("(rewrite) ") : ""}` + (method && method !== "GET" ? color(method) + " " : "") + url + ` ` + (reqTime ? dim(Math.round(reqTime) + "ms") : "");
}

// ../../node_modules/.pnpm/astro@6.0.1_@types+node@24.10.13_jiti@2.6.1_lightningcss@1.31.1_rollup@4.55.2_tsx@4.21._dedb2502a3da963a245a4af4094aacf2/node_modules/astro/dist/core/app/dev/app.js
var DevApp = class extends BaseApp {
  logger;
  constructor(manifest2, streaming = true, logger) {
    super(manifest2, streaming, logger);
    this.logger = logger;
  }
  createPipeline(streaming, manifest2, logger) {
    return NonRunnablePipeline.create({
      logger,
      manifest: manifest2,
      streaming
    });
  }
  isDev() {
    return true;
  }
  /**
   * Updates the routes list when files change during development.
   * Called via HMR when new pages are added/removed.
   */
  updateRoutes(newRoutesList) {
    this.manifestData = newRoutesList;
    ensure404Route(this.manifestData);
  }
  match(request) {
    return super.match(request, true);
  }
  async devMatch(pathname) {
    const matchedRoute = await matchRoute(
      pathname,
      this.manifestData,
      this.pipeline,
      this.manifest
    );
    if (!matchedRoute) return void 0;
    return {
      routeData: matchedRoute.route,
      resolvedPathname: matchedRoute.resolvedPathname
    };
  }
  async renderError(request, {
    skipMiddleware = false,
    error,
    status,
    response: _response,
    ...resolvedRenderOptions
  }) {
    if (isAstroError(error) && [MiddlewareNoDataOrNextCalled.name, MiddlewareNotAResponse.name].includes(error.name)) {
      throw error;
    }
    const renderRoute = async (routeData) => {
      try {
        const preloadedComponent = await this.pipeline.getComponentByRoute(routeData);
        const renderContext = await this.createRenderContext({
          locals: resolvedRenderOptions.locals,
          pipeline: this.pipeline,
          pathname: this.getPathnameFromRequest(request),
          skipMiddleware,
          request,
          routeData,
          clientAddress: resolvedRenderOptions.clientAddress,
          status,
          shouldInjectCspMetaTags: false
        });
        renderContext.props.error = error;
        const response = await renderContext.render(preloadedComponent);
        if (error) {
          this.logger.error("router", error.stack || error.message);
        }
        return response;
      } catch (_err) {
        if (skipMiddleware === false) {
          return this.renderError(request, {
            ...resolvedRenderOptions,
            status: 500,
            skipMiddleware: true,
            error: _err
          });
        }
        throw _err;
      }
    };
    if (status === 404) {
      const custom404 = getCustom404Route(this.manifestData);
      if (custom404) {
        return renderRoute(custom404);
      }
    }
    const custom500 = getCustom500Route(this.manifestData);
    if (!custom500) {
      throw error;
    } else {
      return renderRoute(custom500);
    }
  }
  logRequest({ pathname, method, statusCode, isRewrite, reqTime }) {
    if (pathname === "/favicon.ico") {
      return;
    }
    this.logger.info(
      null,
      req({
        url: pathname,
        method,
        statusCode,
        isRewrite,
        reqTime
      })
    );
  }
};

// ../../node_modules/.pnpm/astro@6.0.1_@types+node@24.10.13_jiti@2.6.1_lightningcss@1.31.1_rollup@4.55.2_tsx@4.21._dedb2502a3da963a245a4af4094aacf2/node_modules/astro/dist/core/app/entrypoints/virtual/dev.js
var currentDevApp = null;
var createApp = ({ streaming } = {}) => {
  const logger = createConsoleLogger(manifest.logLevel);
  currentDevApp = new DevApp(manifest, streaming, logger);
  if (import.meta.hot) {
    import.meta.hot.on("astro:routes-updated", async () => {
      if (!currentDevApp) return;
      try {
        const { routes: newRoutes } = await import("virtual:astro:routes");
        const newRoutesList = {
          routes: newRoutes.map((r) => r.routeData)
        };
        currentDevApp.updateRoutes(newRoutesList);
      } catch (e) {
        logger.error("router", `Failed to update routes via HMR:
 ${e}`);
      }
    });
    import.meta.hot.on("astro:content-changed", () => {
      if (!currentDevApp) return;
      currentDevApp.pipeline.routeCache.clearAll();
    });
  }
  return currentDevApp;
};
export {
  createApp
};
//# sourceMappingURL=astro_app_entrypoint_dev.js.map
